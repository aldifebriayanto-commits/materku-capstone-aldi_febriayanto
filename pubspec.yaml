name: materku
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.1.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # State Management
  provider: ^6.1.1

  # Database
  sqflite: ^2.3.0
  path: ^1.8.3

  # HTTP & API
  http: ^1.1.2
  dio: ^5.4.0

  # UI Components
  cupertino_icons: ^1.0.2
  # flutter_local_notifications: ^14.1.0  # ← DISABLED: Conflict dengan Android SDK 34
  fl_chart: ^0.65.0

  # File Handling
  file_picker: ^8.1.6          # ← FIXED: Upgrade dari 6.2.1
  image_picker: ^1.0.7
  path_provider: ^2.1.1
  open_filex: ^4.3.4

  # Utilities
  intl: ^0.19.0
  shared_preferences: ^2.2.2
  url_launcher: ^6.2.2
  share_plus: ^7.2.1
  permission_handler: ^11.1.0

  # Performance Optimization
  cached_network_image: ^3.3.1
  flutter_cache_manager: ^3.3.1

  # Security
  crypto: ^3.0.3
  encrypt: ^5.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  # Uncomment jika ada assets
  # assets:
  #   - assets/images/
  #   - assets/icons/

  # Fonts (optional)
  # fonts:
  #   - family: Roboto
  #     fonts:
  #       - asset: fonts/Roboto-Regular.ttf
  #       - asset: fonts/Roboto-Bold.ttf
  #         weight: 700